<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tài khoản của tôi</title>
    <link rel="stylesheet" href="/css/root.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link rel="stylesheet" href="/css/createProduct.css">
    <!-- datatable -->
    <link rel="stylesheet" type="text/css"
        href="https://cdn.datatables.net/v/dt/dt-1.12.1/rg-1.2.0/datatables.min.css" />
    <link rel="stylesheet" type="text/css"
        href="https://cdn.datatables.net/searchpanes/2.0.1/css/searchPanes.dataTables.min.css" />
    <link rel="stylesheet" type="text/css"
        href="https://cdn.datatables.net/select/1.4.0/css/select.dataTables.min.css" />
    <link rel="stylesheet" type="text/css"
        href="https://cdn.datatables.net/responsive/2.3.0/css/responsive.dataTables.min.css" />
    <link rel="stylesheet" type="text/css"
        href="https://cdn.datatables.net/buttons/2.2.3/css/buttons.dataTables.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <!-- Toastify alert -->
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css" />

    <!-- css -->
    <link rel="stylesheet" href="/css/dataTables.css">
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        form {
            background-color: #f2f2f2;
            padding: 20px;
            border-radius: 10px;
        }

        h2 {
            text-align: center;
        }

        label {
            display: inline-block;
            margin-bottom: 5px;
        }

        input[type="text"],
        input[type="password"],
        input[type="email"],
        input[type="tel"] {
            padding: 10px;
            border: none;
            border-radius: 5px;
            margin-bottom: 10px;
            width: 100%;
        }
        input[type="password"]{
            border: 1px solid #ccc;
        }

        .Update,
        .changePassWord,
        .Save {
            background-color: var(--prymary-color);
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
            width: 120px;
            text-align: center;
            outline: none;
        }
        .changePassWord{
            width: 100%;
        }

        .Update:hover,
        .changePassWord:hover,
        .Save:hover {
            background-color: var(--prymary-color);
            opacity: 0.8;
        }
    </style>
</head>

<body style="position: relative;">
    <div class="nav" style="position: fixed;height: 100%; z-index: 1;">
        <h1>Dashboard</h1>
        <ul id="route">
            <li><a href="/admin/dashboard">Dashboard</a></li>
            <li><a href="#">Tài khoản của tôi</a></li>
            <li><a href="#">Danh sách Products</a></li>
            <li><a href="/product/create">Thêm Product</a></li>
            <li><a href="#">Products</a></li>
            <li><a href="#">Reports</a></li>

        </ul>
    </div>

    <div class="containerBox" style="position: relative;">
        <div class="nav">

        </div>
        <div class="main" style="width: 80%;height: 100%;padding-bottom: 60px;">
            <h1 style="margin-left: 30px;margin-top: 30px;">Tài khoản của tôi</h1>
            <div class="card" style="width: 95%;margin: auto;">
                <form id="FormSubmit">
                    <label for="fullname">Full Name:</label>
                    <input type="text" id="fullname" name="username" disabled><br>

                    
                    <label for="phone">Phone:</label>
                    <input type="tel" id="phone" name="phone" disabled><br>
                    
                    <label for="address">Address:</label>
                    <input type="text" id="address" name="address" disabled><br>
                    
                    <label for="email">Email:</label>
                    <input type="email" id="email" name="email" disabled><br>

                    <label for="role">Role:</label>
                    <input type="text" id="role" name="role" disabled><br>
                    <div style="display: flex;">
                        <div>
                            <input type="click" class="Update" value="Update">
                            <input type="submit" class="Save" value="Save" style="display: none;" disabled>
                        </div>
                        <div style="margin-left: 10px;">
                            <input type="button" data-bs-toggle="modal" data-bs-target="#exampleModalChangePassword" class="changePassWord" value="Đổi mật khẩu">
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <div class="modal fade" id="exampleModalChangePassword" tabindex="-1" aria-labelledby="exampleModalLabel"
    aria-hidden="true">
    <div class="modal-dialog">
        <form id="formChangePassword" style="padding: 0;">
            <div class="modal-content" style="padding: 20px;">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Đổi mật khẩu</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body" style="position: relative;">
                    <div style="margin-bottom: 16px;">
                        <div style="position: relative;">
                            <p class="mt-2" style="margin-bottom: 0;font-weight:500;color: rgba(0, 0, 0, 0.553);">Mật khẩu</p>
                            <input id="password" style="width: 100%;" type="password"name="oldPW" required>
                            <span style="padding: 2px;cursor: pointer;position:absolute; top:58%; right:2%;font-size:16px"><i id="show-btn1"class="far fa-eye"></i></span>
                        </div>
                        <div style="position: relative;">
                            <p class="mt-2" style="margin-bottom: 0;font-weight:500;color: rgba(0, 0, 0, 0.553);">Mật khẩu Mới</p>
                            <input id="newPassword" style="width: 100%;"name="newPW" type="password"required>
                            <span style="padding: 2px;cursor: pointer;position:absolute; top:58%; right:2%;font-size:16px"><i id="show-btn2"class="far fa-eye"></i></span>
                        </div>
                        <div style="position: relative;">
                            <p class="mt-2" style="margin-bottom: 0;font-weight:500;color: rgba(0, 0, 0, 0.553);">Nhập lại khẩu Mới</p>
                            <input id="changeNewPassword" name="repeatNewPW" style="width: 100%;" type="password"required>
                            <span style="padding: 2px;cursor: pointer;position:absolute; top:58%; right:2%;font-size:16px"><i id="show-btn3"class="far fa-eye"></i></span>
                        </div>
                    </div>
                    <span class="checkPassword" style="position: absolute;bottom: 10px;;font-size:14px;color: red; display: none;">Mật khẩu mới không giống nhau</span>
                </div>
                <div class="modal-footer">
                    <button type="submit" id="btn-changePassword" class="btn btn-primary" style="padding: 6px 12px;">Change</button>
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                </div>
            </div>
        </form>
    </div>
</div>
</body>

<script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>


<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
<script src="/js/route.js"></script>
<script>
    let height = window.innerHeight;
    $(".main")[0].style.minHeight = height + "px";
    let html = route.map((e, index) => {
        const pathname = window.location.pathname
        if (e.href == pathname) {
            return `<li class="active"><a href="${e.href}">${e.text}</a></li>`
        }
        else {
            return `<li><a href="${e.href}">${e.text}</a></li>`
        }

    })
    $("#route")[0].innerHTML = html.join("");

</script>

<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>

<!-- datatables -->
<script type="text/javascript" src="https://cdn.datatables.net/v/dt/dt-1.12.1/rg-1.2.0/datatables.min.js"></script>
<script type="text/javascript"
    src="https://cdn.datatables.net/searchpanes/2.0.1/js/dataTables.searchPanes.min.js"></script>
<script type="text/javascript" src="https://cdn.datatables.net/select/1.4.0/js/dataTables.select.min.js"></script>
<script type="text/javascript"
    src="https://cdn.datatables.net/responsive/2.3.0/js/dataTables.responsive.min.js"></script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
    crossorigin="anonymous"></script>

<!-- toastify -->
<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>

<script>
    $.ajax({
        url: `/user/getme`,
        type: "GET",
        success: (response) => {
            console.log(response)
            const data = response.data
            $("#fullname").val(data.username)
            $("#email").val(data.email)
            $("#address").val(data.address)
            $("#phone").val(data.phone)
            $("#role").val(data.UserRoles[0].Role.name)
            $(".Update").click(e => {
                $("#fullname")[0].disabled = false
                $("#phone")[0].disabled = false
                $("#address")[0].disabled = false
                $(".Update")[0].disabled = true
                $(".Save")[0].disabled = false
                $(".Save")[0].style.display = "block"
                $(".Update")[0].style.display = "none"
            })

            $(".changePassWord").click(e => {
                $("#exampleModalChangePassword").modal('show');
            })
            $("#FormSubmit").submit(e => {
                e.preventDefault();
                const dataForm = $("#FormSubmit").serialize();
                $.ajax({
                    url: `/user`,
                    type: "PATCH",
                    data:dataForm,
                    success: (response) => {
                        Toastify({
                            text: response?.message,
                            duration: 2000,
                            newWindow: true,
                            offset: {
                                x: 50, // horizontal axis - can be a number or a string indicating unity. eg: '2em'
                                y: 10 // vertical axis - can be a number or a string indicating unity. eg: '2em'
                            },
                            gravity: "top", // `top` or `bottom`
                            position: "right", // `left`, `center` or `right`
                            stopOnFocus: true, // Prevents dismissing of toast on hover
                            style: {
                                background: "linear-gradient(to right, green, green)",
                                width: "250px",
                                fontSize: "16px"
                            },
                        }).showToast();
                        $("#fullname")[0].disabled = true
                        $("#phone")[0].disabled = true
                        $("#address")[0].disabled = true
                        $(".Update")[0].disabled = false
                        $(".Save")[0].disabled = true
                        $(".Save")[0].style.display = "none"
                        $(".Update")[0].style.display = "block"
                    },
                    error: (error) => {
                        Toastify({
                            text: error?.responseJSON?.errorCode,
                            duration: 2000,
                            newWindow: true,
                            //close: true,
                            offset: {
                                x: 50, // horizontal axis - can be a number or a string indicating unity. eg: '2em'
                                y: 10 // vertical axis - can be a number or a string indicating unity. eg: '2em'
                            },
                            gravity: "top", // `top` or `bottom`
                            position: "right", // `left`, `center` or `right`
                            stopOnFocus: true, // Prevents dismissing of toast on hover
                            style: {
                                background: "linear-gradient(to right, red, red)",
                                width: "250px",
                                fontSize: "16px"
                            },
                        }).showToast();
                    }
                })
            })
            $("#formChangePassword").submit(e=>{
                e.preventDefault();
                const data = $("#formChangePassword").serialize();

                const password = $("#password").val();
                const newPassword = $("#newPassword").val();
                const changeNewPassword = $("#changeNewPassword").val();
                if(password==""||newPassword==""||changeNewPassword==""){
                    $(".checkPassword")[0].innerText="Mật khẩu không được để trống"
                    $(".checkPassword")[0].style.display="block"
                }
                else if(newPassword.length<10||changeNewPassword.length<10){
                    $(".checkPassword")[0].innerText="Mật khẩu mới phải có độ dài lớn hơn 10"
                    $(".checkPassword")[0].style.display="block"

                }
                else if(newPassword!=changeNewPassword){
                    $(".checkPassword")[0].innerText="Mật khẩu mới không giống nhau"
                    $(".checkPassword")[0].style.display="block"
                }
                else {
                    $(".checkPassword")[0].style.display="none"
                    console.log(data)
                    $.ajax({
                    url: `/auth/updatePW`,
                    type: "PATCH",
                    data:data,
                    success: (response) => {
                        Toastify({
                            text: response?.message,
                            duration: 2000,
                            newWindow: true,
                            offset: {
                                x: 50, // horizontal axis - can be a number or a string indicating unity. eg: '2em'
                                y: 10 // vertical axis - can be a number or a string indicating unity. eg: '2em'
                            },
                            gravity: "top", // `top` or `bottom`
                            position: "right", // `left`, `center` or `right`
                            stopOnFocus: true, // Prevents dismissing of toast on hover
                            style: {
                                background: "linear-gradient(to right, green, green)",
                                width: "250px",
                                fontSize: "16px"
                            },
                        }).showToast();
                        $("#exampleModalChangePassword").modal('hide');
                    },
                    error: (error) => {
                        Toastify({
                            text: error?.responseJSON?.errorCode,
                            duration: 2000,
                            newWindow: true,
                            //close: true,
                            offset: {
                                x: 50, // horizontal axis - can be a number or a string indicating unity. eg: '2em'
                                y: 10 // vertical axis - can be a number or a string indicating unity. eg: '2em'
                            },
                            gravity: "top", // `top` or `bottom`
                            position: "right", // `left`, `center` or `right`
                            stopOnFocus: true, // Prevents dismissing of toast on hover
                            style: {
                                background: "linear-gradient(to right, red, red)",
                                width: "250px",
                                fontSize: "16px"
                            },
                        }).showToast();
                    }
                })
                }
            })
        },
        error: (data) => {
            Toastify({
                text: data.responseJSON.errorCode,
                duration: 2000,
                newWindow: true,
                //close: true,
                offset: {
                    x: 50, // horizontal axis - can be a number or a string indicating unity. eg: '2em'
                    y: 10 // vertical axis - can be a number or a string indicating unity. eg: '2em'
                },
                gravity: "top", // `top` or `bottom`
                position: "right", // `left`, `center` or `right`
                stopOnFocus: true, // Prevents dismissing of toast on hover
                style: {
                    background: "linear-gradient(to right, red, red)",
                    width: "250px",
                    fontSize: "16px"
                },
            }).showToast();
        }
    })

</script>

</html>